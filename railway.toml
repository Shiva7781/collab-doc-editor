[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "node_modules/.bin/tsx ws-server.ts"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
